swagger: "2.0"
info:
  title: "SSRF Test - Internal Network Scan"
  description: |
    Testing internal endpoints via Swagger UI
  version: "1.0"
host: "http://internal-server.local"
paths:
  /admin:
    get:
      summary: "Internal Admin Panel"
      responses:
        '200':
          description: "Admin access"
